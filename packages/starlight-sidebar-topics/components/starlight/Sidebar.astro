---
import { StarlightSidebarTopicsLocalsSymbol, type StarlightSidebarTopicsLocals } from '../../libs/locals'
import Topics from '../Topics.astro'

const { hasSidebar } = Astro.locals.starlightRoute

// @ts-expect-error - See `libs/locals` for more information.
const currentTopic = Astro.locals[StarlightSidebarTopicsLocalsSymbol] as StarlightSidebarTopicsLocals
---

{hasSidebar && currentTopic && <Topics current={currentTopic.config} />}
